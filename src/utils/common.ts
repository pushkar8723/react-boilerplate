/**
 * Utility class for functions used across application.
 */
export default class Common {
    /**
     * Checks if user is authorized.
     */
    public static isAuthorized(): boolean {
        // TODO: implement authentication rule.
        return true;
    }
}
